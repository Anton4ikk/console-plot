{
  "name": "console-plot-tests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "jest --config=config/jest.config.js",
    "test:watch": "jest --config=config/jest.config.js --watch",
    "test:unit": "jest --config=config/jest.config.js unit/",
    "test:integration": "jest --config=config/jest.config.js integration/ && cd integration/installed && npm install && npm test",
    "test:installed": "cd integration/installed && npm install && npm test"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.28.0",
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "babel-jest": "^30.0.4",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "console-plot": "file:.."
  }
}
